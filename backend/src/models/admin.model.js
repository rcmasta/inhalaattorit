const db = require('../config/db');

const dbAdd = () => {
    try {
        const query = "";
        
    } catch (err) {
        throw err;
    }
};

const dbEdit = () => {
    try {
        const query = "";
        
    } catch (err) {
        throw err;
    }
};

const dbRemove = () => {
    try {
        const query = "";
        
    } catch (err) {
        throw err;
    }
};

module.exports = { dbAdd, dbEdit, dbRemove }