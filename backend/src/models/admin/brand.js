const db = require('../../config/db');

class brand {

    static create = (body) => {

    };

    static edit = (id, body) => {

    };

    static delete = (id) => {

    };
};

module.exports = brand;